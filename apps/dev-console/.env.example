# Show the "AI Assistant" area: the in-app assistant (bring your own LLM API key; proposes +
# applies workflow edits) plus an "Advanced" section for external-agent setup.
# Hidden when this flag is missing or set to anything other than "true".
VITE_FEATURE_FLAG_AGENT=false
